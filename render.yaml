services:
  - type: web
    name: order-agent
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free

    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
      - key: ORDER_TOKEN
        value: demo-token-123   # change this in Render dashboard → Environment
